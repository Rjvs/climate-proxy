"""Options flow schemas — re-use config schemas for sensor steps."""

from __future__ import annotations

# Options flow uses the same schemas as the config flow sensor steps.
# See config.py for the schema functions.

__all__: list[str] = []
